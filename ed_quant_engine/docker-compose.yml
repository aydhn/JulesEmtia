version: '3.8'
services:
  quant_engine:
    build: .
    container_name: edcapital_quant
    restart: always
    environment:
      - TZ=Europe/Istanbul
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
